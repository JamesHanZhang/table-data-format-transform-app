################ CSV相关参数(常用修改) ##################
# 加载时CSV的默认分隔符，如为xls格式，则不要动它：input_sep=','
input_sep=','

# 传出CSV的默认分隔符: output_sep=','
output_sep=','

################ EXCEL相关参数(常用修改) ################
# 默认导入的EXCEL的子表名，如不涉及请勿修改
input_sheet = "Sheet1"

# 默认导出EXCEL的子表名，如不涉及请勿更改
output_sheet = "Sheet1"

################ 导入导出相关参数(常用修改) ################
# 以下可添加绝对路径，如为空值""，则为默认导入路径input_dataset，也可添加默认路径下input_dataset的子文件夹，例如:
# 如添加子文件夹child_folder/split1，则实际路径为:../input_dataset/child_folder/split1
input_path = ""

# 以下可添加绝对路径，如为空值""，则为默认导出路径output_dataset，也可添加默认路径下output_dataset的子文件夹，例如:
# 如添加子文件夹child_folder/split1，则实际路径为:../output_dataset/child_folder/split1
output_path = ""

